<project_bank>
You may use these projects directly or modify their descriptions and tech stacks to align with the job description. Treat them as a pool of raw material — rewrite bullets, swap technologies, and inject JD keywords following all rules in the system prompt.

<project id="1">
<name>ZeroFrame</name>
<tech>C++, Go, Python, gRPC, ONNX Runtime, CUDA, Shared Memory</tech>
<bullets>
- Engineered a decentralized microservices pipeline that decouples video ingestion, orchestration, and inference into independent processes, enabling asynchronous workload distribution across an N-node concurrent worker pool.
- Implemented a zero-copy Inter-Process Communication (IPC) protocol utilizing a 10-slot memory-mapped ring buffer, managing a contiguous 62MB block in shared memory to completely bypass gRPC serialization overhead for large payloads.
- Developed a hardware-accelerated C++ inference engine using ONNX Runtime and CUDA to execute INT8 quantized models, performing in-place memory mutations to minimize cache misses and avoid redundant data copying.
- Architected a fault-tolerant gRPC orchestration layer with a 500ms failover mechanism, ensuring continuous video uptime by implementing a raw-frame pass-through protocol during C++ worker node crashes.
</bullets>
</project>

<project id="2">
<name>CudaSwarm</name>
<tech>Go, C++, gRPC, Docker Compose, NVML</tech>
<bullets>
- Constructed a local-first ML orchestrator, simulating a 4-node distributed cluster (1 master, 3 workers) via Docker Compose to dispatch and manage PyTorch transfer learning workloads without cloud dependencies.
- Bridged Go and C++ via cgo to wrap NVIDIA Management Library (NVML) APIs, streaming hardware-level VRAM telemetry to the master at strict 1-second intervals to prevent container Out-Of-Memory crashes.
- Implemented a VRAM-aware scheduling algorithm that evaluates incoming ML payloads against real-time worker telemetry, achieving less than 60ms scheduling latency from job submission to node allocation.
- Architected a remote workload execution engine to spawn training scripts, establishing gRPC server-streaming pipelines to capture and pipe 100\% of distributed stdout/stderr logs back to the master in real time.
</bullets>
</project>

<project id="3">
<name>ViT Triton Accelerator</name>
<tech>Python, PyTorch, OpenAI Triton, GPU Memory Tiling, Autograd</tech>
<bullets>
- Orchestrated a fused Flash Attention mechanism leveraging runtime SRAM autotuning across 3 distinct custom Triton kernels (Forward, Preprocess, Backward), achieving a 1.08x speedup over PyTorch's native C++ backend.
- Modeled an IO-aware backward pass utilizing on-the-fly gradient recomputation and Log-Sum-Exp state tracking, mathematically reducing HBM spatial complexity from O(N\^{}2) to O(N) to safely enable long-sequence training.
</bullets>
</project>
</project_bank>